#include <stdio.h>

/**
 *  main - Entry point
 *
 *  Description: Use of printf to print a line of text
 *
 *  Return: Always 0 (success)
 */
int main(void)
{
	char *ptr = "with proper grammar, but the outcome is a piece of art,";
	printf ("%s",ptr);
	return (0);
}
