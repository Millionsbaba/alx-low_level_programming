#include "main.h"

/**
 * _islower - A new code to print lower case c
 * @c: A character argument
 *
 * Return: 1 or 0 based on the condition
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
